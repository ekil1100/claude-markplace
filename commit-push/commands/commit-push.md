---
description: Commit all changes and push to remote
---

1. Run `git status` and `git diff` to review changes
2. Create a commit following the repo's commit message conventions
3. Push to the remote branch

If $ARGUMENTS is provided, use it as the commit message. Otherwise, generate an appropriate message based on the changes.
